# core
customtkinter>=5.2.0

# voice (optional - app runs without these)
SpeechRecognition>=3.10.0
PyAudio>=0.2.13
pyttsx3>=2.90

# real app icons (optional - emoji fallback without these)
pywin32>=306
Pillow>=10.0.0

# faster typo correction (optional - difflib fallback without it)
rapidfuzz>=3.0.0

# AI semantic search (optional - app runs without these)
# Ollama must be installed separately: https://ollama.com
# Pull the embedding model: ollama pull nomic-embed-text
# Pull the chat model:       ollama pull qwen2.5:7b
#
# PDF text extraction (any one of these will work):
pdfplumber>=0.10.0
# pdfminer.six>=20221105   # alternative
# pymupdf>=1.23.0          # another alternative (install as: pip install pymupdf)
#
# Word document extraction:
python-docx>=1.0.0
