"""REMOVED for read-only safety.

FileMind is 100% read-only. The old organizer (which moved files into
category folders and into a trash folder) has been removed entirely so
that no code path in this project can delete, move, rename, relocate,
or modify any file.

Allowed actions are only: search, open file, open folder location,
open app, open website, search web.
"""

# Intentionally empty. Do not add file-modifying code here.
